# 排查快照（供专家审核）

> 生成时间：2026-09-21 ｜ 代码版本：本地工作区（HEAD=`eabb8b8` + 未提交 H15.5.3 completion 修复）
> 测试范围：18 例测试集子集 T01 / T03 / T07 / T11 / T13 / T15，进程内 `runCase` 实测。

## 一、结论速览

| 用例 | 结果 | 判定 |
| --- | --- | --- |
| T01 月经后期 | 血寒（虚寒）→ 大营煎加减 | ✅ 命中预期 |
| T03 子宫肌瘤术后 | 气虚血瘀 → 桂枝茯苓丸加味 | ⚠️ 辨证偏差 |
| T07 便秘 | `EXECUTION_INCOMPLETE`（缺 formulaSelection/formulaReview） | ❌ 未收敛 |
| T11 月经失调膏方 | 脾肾两虚气血不足，treatmentFormDecision=膏方（以膏代煎） | ✅ 膏方已兑现 |
| T13 肺结核膏方 | 气阴两虚，treatmentFormDecision=膏方（含收膏工艺/用法） | ✅ 膏方已兑现 |
| T15 痛经针灸 | 肝郁气滞（气滞型痛经），未强制方剂 | ✅ 命中预期 |

## 二、问题定位

### 1. T07 便秘未收敛（真实 bug）
- 现象：`conversation` + `EXECUTION_INCOMPLETE: missing artifacts [formulaSelection, formulaReview]`。
- 定位：`formula.search_candidates` 被调用 6 次，但 `runMetrics.selectedCandidateRef` 始终为空、`formulaReviewRecorded=false`；检索诊断显示多次 `formula.search_candidates` 的 query 为空串。
- 推测：对「老年阴虚肠燥便秘」未返回可采纳候选方，或候选未进入 selection；completion 在 formulaSelection 缺失时直接终止而非进入 COMPLETION_RECOVERY。
- 审核重点：`src/adapters/ai-sdk/agent-runtime.ts`（completion/closure 判定）+ `formula.search_candidates` 工具绑定与候选采纳逻辑。

### 2. 膏方链路（已兑现，非 bug）
- T11/T13 均正确激活 `gaofang` + 加载 `gaofang-reasoning` skill，`treatmentPlan.treatmentFormDecision.form` 记录为「膏方（以膏代煎）」。
- T13 含完整收膏内容（阿胶收膏、每日 1 匙冲服、禁忌）；T11 的 `advisoryComposition` 为空——待确认是 GF 资产不含组成还是模型未回填（次要）。
- 审核重点：`capabilities/gaofang/` + `skills/gaofang-reasoning/SKILL.md` + treatmentFormDecision 与最终 formula 剂型的映射。

### 3. T03 辨证偏差
- 现象：预期「肝郁脾虚为主导、既往血瘀为病史/兼夹」，实际主证判「气虚血瘀」。
- 推测：将既往「色黯血块/剧痛」的瘀血证据升格为当前主证，或「当前 vs 历史」时序建模不足。
- 审核重点：`src/platform/workspace/pattern-assessment`（若有）与 understanding 的 temporalRole 处理、`prompts/clinical-primary/prompt.md` 中对「当前 vs 历史」的引导。

### 4. T03 偶发 JSON 解析失败
- 现象：首次运行 5.6s 报 `Expected ',' or '}' after property value in JSON at position 3613`，重跑成功。
- 推测：deepseek-chat 结构化输出偶发 JSON 语法错误；proposal 解析失败后的重试机制未生效。
- 审核重点：`src/adapters/ai-sdk/minimal-finalization.ts` / `proposal-canonicalizer.ts` 的 JSON 解析与重试逻辑（`runMetrics.proposalParseFailureCount`）。

## 三、打包内容结构

- `src/`：运行时全链路（understanding → planner → agent → retrieval → workspace → authority → server）
- `skills/`：5 个 skill（tcm-clinical-cognition / clinical-reasoning-governor / gaofang-reasoning / general-clinical-reasoning / tcm-clinical-reasoning）
- `prompts/`：clinical-primary / clinical-primary-classic
- `capabilities/`：gaofang / tcm-core / tcm.external-therapy / tcm.preparation
- `tests/`：契约与回归测试
- 根目录：架构规则 `pgy_code_rules.md`、`开发架构.md`、`CURRENT-SYSTEM-SNAPSHOT.md`、`H15.5 收敛计划`、`README.md`、本快照
- 附录：`reports/18例子集-全过程链路记录.md`（6 例全链路原始记录）
