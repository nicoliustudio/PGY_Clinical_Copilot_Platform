# H15.5 — Clinical Execution Convergence & Expert Skill Hardening

> 目标：一次性收敛最近真实病例中已经重复暴露的执行问题。  
> 本轮不是继续扩展临床能力，而是让现有能力更快、更稳、更像专家地完成任务。  
> H15.4 Safety semantics 已冻结，本轮不得重新设计 Safety。

---

## 0. 为什么这一轮要扩大范围

最近真实 UI 病例已经重复出现同一种系统行为：

```text
临床理解已经形成
↓
主证基本明确
↓
治法基本明确
↓
候选/知识已经拿到
↓
仍继续 search / reconsider / clarify
↓
上下文不断膨胀
↓
step limit
↓
resource_limit_fallback
↓
最终结果残缺或草草收尾
```

这已经不是单例偶发，也不是单纯“模型慢”。

当前系统主要问题已经从：

```text
不会做
```

变成：

```text
会做，但不会及时收敛和交付
```

本轮要求同时处理：

1. stateful tool stale cache /错误复用；
2. 不可由当前工具解决的 patient-specific unknown 仍驱动 retrieval；
3. Clinical Planner / WorkingView 信息过重、目标过多、不可满足；
4. Skill 缺少“专家如何收敛”的临床方法；
5. knowledge retrieval 过宽、候选爆炸、非妇科污染；
6. duplicate evidence / candidate 重复写入；
7. tool result echo / context 重复导致 token 高；
8. 特殊能力（尤其 gaofang）“激活了但没有 execution handoff”；
9. clarification 被过度使用，吞掉已经成立的 clinical advisory；
10. Agent mode / completion decision 对已形成临床结果仍过度依赖模型自律；
11. capability discover/activate 等固定 orchestration tax 是否仍有必要；
12. 最终结果必须从“信息收集模式”切换到“临床交付模式”。

本轮不是增加复杂度，而是删除无效复杂度。

---

# 1. 本轮总原则

唯一总原则：

> **让模型每一步只接触当前临床决策真正需要的信息，只执行有现实可能改变临床结论的动作；一旦足以形成医生可审阅结果，就及时提交。**

进一步拆成六条：

### 1.1 临床状态完整，模型工作集精简

```text
Workspace = 完整病历、完整推理状态、完整 provenance
WorkingView = 当前真正参与下一决策的工作集
```

不得因为 Workspace 完整，就每轮把全部历史重新塞给模型。

---

### 1.2 Retrieval 必须服务当前决策，而不是积累背景资料

任何新增检索前，Agent 应能回答：

```text
这个检索可能改变什么？

- primary pattern？
- treatment principle？
- base formula？
- safety/review disposition？
- 关键 modification？
- gaofang stage decision？
```

若无法改变任何一项，不应继续检索。

---

### 1.3 不可由工具解决的未知，不是检索任务

例如：

```text
血氧
CRP
血象
病原学
近期影像
患者当前用药
过敏史
肝肾功能
```

若输入未提供、当前工具又无法取得：

```text
→ 保留为 missing information / uncertainty / review reason
→ 不再通过 knowledge.search 反复尝试解决
```

---

### 1.4 临床不确定性不等于“不能交付”

当：

```text
病 / 证 / 法 / 候选方
```

已经足以形成医生可审阅建议，而剩余未知只影响：

```text
复核
安全
随访
后续分期
剂型适宜性
```

则应该提交：

```text
clinical advisory
+ missing information
+ reviewRequired（如适用）
```

不得仅因不完美完整而退化成 clarification-only。

---

### 1.5 特殊能力必须有 execution handoff

Capability 激活不是完成。

```text
Capability activated
Knowledge retrieved
Skill loaded
```

最终必须落到可提交的临床决策。

尤其 gaofang 必须能明确形成：

```text
CURRENTLY_SUITABLE
CURRENTLY_NOT_SUITABLE
TREAT_FIRST_THEN_GAOFANG
```

或现有架构中的等价表达。

---

### 1.6 Kernel 只管不变量；收敛策略由 Agent/Skill 负责

不要新增：

```text
固定第 N 步提交
最多 search N 次
硬编码疾病→流程
EarlyStop Engine
Planner subsystem
Multi-Agent
Tree Search
```

收敛必须来自：

```text
更好的信息选择
更好的工具语义
更好的专家 Skill
更少的系统回声
```

---

# 2. P0 — Stateful Tool Cache Correctness

这是本轮第一优先级，先排查再继续其他修复。

## 2.1 审计范围

至少检查：

```text
formula.search_candidates
```

以及所有：

```text
tool output depends on Workspace / WorkingView
```

的工具。

重点确认当前 cache key 是否仅使用：

```text
tool name + explicit args
```

如果 Tool 隐式读取当前 clinical state，那么：

```text
相同 args ≠ 相同 query
```

例如：

```text
state v1:
primaryPattern = A
treatment = X

formula.search_candidates(topK=5)

之后：

state v2:
primaryPattern = B
treatment = Y

formula.search_candidates(topK=5)
```

第二次不得因为 `topK=5` 相同而直接复用 v1。

## 2.2 正确修法

优先选择最简单方案：

### Option A
stateful tools 不做跨-state cache。

### Option B
cache key 加：

```text
workspaceRevision
```

或最小 relevant-state fingerprint：

```text
primaryPattern
treatmentPrinciple
selectedHypothesis
focusedCandidates
formulaSelection state
```

不要构造复杂通用 cache framework。

## 2.3 静态工具继续允许缓存

例如结果完全由显式 ref 决定时：

```text
knowledge.get_source(sourceId)
knowledge.get_asset(assetId)
formula.get_evidence(candidateRef)
```

可以安全复用。

## 2.4 验收

必须新增 deterministic test：

```text
state v1 → search_candidates
change clinically relevant state
state v2 → search_candidates

EXPECT:
second result is recomputed or keyed by new state
not stale reused
```

如果审计发现当前没有 stale cache，则报告证据，不为了“做事情”强行改。

---

# 3. P1 — Planner / WorkingView 收敛重构

本轮可以改 Clinical Planner / WorkingView，但只能做减法。

## 3.1 当前问题

Planner 现在经常生成：

```text
goal
decisionQuestion
criticalEvidenceNeeds × 6~8
stopWhen × 5~6
uncertainty × 5~8
```

其中相当一部分是：

```text
当前工具根本无法获得的患者信息
```

这使 Agent 长时间保持：

```text
“任务仍未完成”
```

的心理状态。

## 3.2 新目标

Planner 不再承担“大段计划说明书”。

建议缩成紧凑的 Current Clinical Workset：

```text
CurrentDecisionFocus
LeadingClinicalState
CompetingAlternative(s)
DecisionChangingUnknowns
ToolResolvableUnknowns
NonToolResolvableUnknowns
StopSignal
```

不要求增加新的 durable Workspace schema。

如果能通过 WorkingView projection 临时生成，优先临时 projection。

## 3.3 CurrentDecisionFocus

示例：

```text
当前主要分歧：
气虚不固 vs 血热/瘀阻是否足以改变基础方

需要解决的关键证据：
当前出血色质、腹痛性质、舌脉热象权重
```

不是：

```text
继续搜索这个病所有可能病因、所有证型、所有方剂
```

## 3.4 Unknown 分类

每个 decision-changing unknown 至少能区分：

```text
toolResolvable = true / false
```

### true
当前 Tool 可以真实增加信息：

```text
规范诊断标准
候选方 source evidence
gaofang card
modification evidence
```

### false
当前 Tool 不可能拿到：

```text
患者最新血氧
患者 CRP
患者 CT
患者培养结果
患者药物过敏史
```

false 只能：

```text
保留
输出
触发 review（若 safety 已规定）
```

不得驱动通用知识检索。

---

# 4. P1 — Expert Skill Hardening

这是本轮重要组成部分。

目标：

> Skill 从“会辨证的指南”升级成“知道怎么像专家一样收敛的临床导师”。

不要把 Skill 改成固定工作流。

只增加高价值专家原则。

---

## 4.1 临床分歧优先，而非支持材料累积

加入语义：

```text
当已有较强 leading pattern 时，
进一步检索应优先用于区分真正会改变治法或基础方的竞争解释。

不要为了每一个可能证型分别补齐一套支持资料。
不要为了让已有结论“证据更多”而继续泛搜。
```

---

## 4.2 区分“证据不足”与“信息缺失但不阻止当前判断”

加入：

```text
若患者特异检查/病史缺失，但现有四诊已足以形成当前中医辨证与医生可审阅建议，
应明确保留不确定性，而不是自动退化为 clarification。
```

---

## 4.3 专家停止原则

加入：

```text
采取下一动作之前，先判断：

如果这个动作得到不同结果，
是否可能改变：
- primary pattern
- treatment principle
- base formula
- major safety/review status
- key modification
- treatment-specific decision

若不会改变，不应继续。
```

注意：

这不是 Tool hard gate。

是专家临床策略。

---

## 4.4 主证稳定后切换模式

Skill 要明确：

```text
主证未稳定
→ 辨证模式

主证已较稳定
→ 方证比较模式

基础方已稳定
→ 个体化/剂型/安全复核模式

结果足够
→ 提交模式
```

这只是认知模式，不实现成 Runtime phase machine。

---

## 4.5 候选收窄后不再泛搜

加入：

```text
当已有 2~3 个真正有临床竞争力的候选时，
优先比较：
- 支持证据
- 反证
- 方证覆盖
- 治法一致性
- patient-specific mismatch

不要重新回到宽泛 disease/formula search。
```

---

## 4.6 “已覆盖不重复”

继续强化：

```text
基础方已经覆盖的临床目标，不因存在额外规则或知识而重复加药。
```

---

## 4.7 “兼证须有证据”

加入：

```text
不得为了合理化某味药、某个候选或某个加减，
反向创造病例未充分支持的病机。

例如：
仅为解释清热药而新增“郁而化热”，
若患者证据不足，则不得成立。
```

---

## 4.8 方证可以双向校正，但不得无限回退

允许：

```text
下游候选证据
→ 反过来挑战上游 pattern
```

但只有当出现真正 decision-changing contradiction 时才回退。

不要因为每个新候选都重新从头辨证。

---

## 4.9 临床克制

加入：

```text
专家性不等于复杂度。

简单病例允许简单结论。
基础方充分时，不必强行加减。
证据不足时，保留不确定性。
没有现实可能改变处方的信息，不继续追索。
```

---

# 5. P1 — Clarification / Clinical Advisory Boundary

H15.4 已解决 safety review semantics。

本轮必须利用这个基础，避免 clarification 吞掉已形成结果。

## 5.1 clarification 的适用条件

clarification 应主要用于：

```text
关键患者信息缺失，
且缺失信息真实阻止形成当前最小临床建议。
```

不是：

```text
任何 remaining uncertainty
```

都 clarification。

## 5.2 若已经形成：

```text
disease
primary pattern
treatment principle
formula candidate / treatment direction
```

且剩余信息只是：

```text
review
monitoring
病因排除
后续检查
疗程调整
剂型最终确认
```

优先：

```text
clinical result
+ missing information
+ reviewRequired
```

而不是 clarification-only。

## 5.3 urgent 仍遵守 Safety

不要降低 H15.4。

urgent 仍可以 block normative commit。

本轮只是修：

```text
non-blocking uncertainty
→ 不应吞掉 advisory
```

---

# 6. P1 — Gaofang Execution Handoff

本轮必须真正闭环。

## 6.1 已知问题

真实病例已经证明：

```text
gaofang capability activated
gaofang-reasoning loaded
GF-002 retrieved
GF-002 expanded
病/证/法已形成
```

但最后：

```text
无 gaofang decision
无 formula selection
clarification
resource_limit_fallback
```

这不允许再次出现。

## 6.2 不新增第二套膏方系统

不要新增：

```text
GaofangPlanner
GaofangAgent
TreatmentModeEngine
```

继续使用现有：

```text
Capability
Skill
Knowledge cards/assets
Workspace
Proposal
```

## 6.3 Skill 必须明确三类临床结论

至少能表达：

### A. 当前适合膏方

```text
currentGaofangSuitability = suitable
```

并给医生：

```text
当前膏方思路 / reference asset / clinical rationale
```

### B. 当前不适合膏方

例如：

```text
明显急性标实
当前先汤剂/其他治疗处理
```

但仍要：

```text
给当前治疗方药 advisory
```

不能“无方”。

### C. 先治标，后转膏

例如：

```text
当前痰热壅肺
先清化平喘
待急性标实缓解
再按肺肾两虚挟痰热内阻进入膏方
```

并给出：

```text
future gaofang reference
```

如 GF-002。

## 6.4 “以膏代煎”的理解

用户/医生明确写：

```text
以膏代煎
```

默认应视为：

```text
明确治疗形式需求
```

不应自动提升为：

```text
必须先 clarification 才能继续
```

如果确实存在：

```text
剂型指定
vs
长期调补膏方
```

的差异，可在输出中说明两种 interpretation，但不得因此吞掉当前临床方药建议。

## 6.5 关键原则

```text
“当前不适宜调补收膏”
≠
“不能给当前治疗方药”
```

必须写进 gaofang Skill。

---

# 7. P1 — Retrieval Quality & Specialty Contamination

## 7.1 当前真实问题

非妇科病例中出现大量：

```text
妊娠咳嗽
妊娠心烦
妊娠痫症
羊水过多
恶阻
产后蓐劳
胎气上逆
妇科梅核气
```

进入 WorkingView。

这不是仅仅“看起来不美观”。

它会造成：

```text
候选膨胀
token 增加
判断成本上升
方向漂移
```

## 7.2 本轮允许做的

先审计已有 metadata：

```text
specialty
disease context
sex
pregnancy context
source applicability
P1 scope
```

如果已有 metadata 足够确定性过滤明显不适用 evidence，则做最小 applicability filtering。

例如：

```text
65岁男性
→ 妊娠病 normative treatment evidence
```

应默认不进入主要 treatment candidate workset。

## 7.3 不允许做的

不要：

```text
写疾病→公式硬映射
大规模补内科知识库
新增一个 specialty routing engine
```

如果现有 metadata 不足以干净解决：

```text
记录为 Knowledge Asset Gap
```

本轮只减少明显污染。

---

# 8. P1 — Knowledge Search / Candidate Surface 收敛

## 8.1 Retrieval ≠ Candidate explosion

`knowledge.search` 的主要职责是：

```text
提供 evidence
```

不应该每次都让大量 formula 自动变成长期 active candidates。

审计：

```text
knowledge.search
→ evidence.added
→ candidate.presented
```

的绑定关系。

如果当前每次检索都会把所有含 formula 的结果注入 candidate surface，需要收紧。

## 8.2 理想语义

```text
knowledge.search
→ evidence

formula.search_candidates
→ compact candidate set
```

如果现有架构无法完全拆开，至少做到：

```text
相同 candidate 不重复 present
明显非当前 specialty/application 的 candidate 不进入 focused workset
```

## 8.3 不要求固定 topK

不要通过：

```text
topK 永远 = 2
```

伪造收敛。

重点是候选有区分力，不是机械少。

---

# 9. P1 — Deterministic De-dup

这个应当纯确定性解决。

## 9.1 Evidence

若 Workspace 已存在同一 canonical evidence id：

```text
不要再次 evidence.added
```

可以记录：

```text
evidence.reused
```

或完全静默。

## 9.2 Candidate

若同一 candidate id 已存在：

```text
不要再次 candidate.presented
```

除非临床状态发生了新的 meaningful assessment。

## 9.3 Source

重复 get_source：

若内容已在 Workspace/context：

```text
返回 compact reused receipt
```

不要再次返回完整 source object。

---

# 10. P1 — Tool Result Echo / Payload Reduction

本轮要实际测。

## 10.1 record_deliberation

当前存在：

```text
input 3k~5k chars
→ output 又 3k~5k chars
```

Agent 已知道自己刚写了什么，不需要 Tool 完整回声。

改为 compact receipt：

```text
{
  accepted: true,
  updatedArtifacts: [...],
  newStateVersion: ...,
  meaningfulDelta: [...],
  unresolvedDecisionItems: [...]
}
```

不要回显完整 payload。

## 10.2 get_source / get_evidence

若 Agent 已经拥有 full evidence：

重复调用只返回：

```text
ref
reused=true
noNewInformation=true
```

或等价 compact representation。

## 10.3 不做通用 Context Compression

本轮不要：

```text
LLM summarizer
history compression model
summary-of-summary
```

只做：

```text
selection
dedup
compact receipts
```

---

# 11. P1 — WorkingView Context Selection

Workspace 不动。

只优化模型每轮看到什么。

## 11.1 每轮应优先给

```text
Current Decision Focus

Current patient evidence relevant to this decision

Primary pattern
Secondary / competing pattern if still live

Current treatment principle

Focused candidate(s)

Key support / contradiction

Tool-resolvable unknowns

Non-tool-resolvable unknowns（简短）

Latest meaningful delta

Completion readiness
```

## 11.2 不应反复给

```text
全部历史 tool payload
全部 rejected candidates
所有旧 search result
已经 resolve 的 uncertainties
完整重复 source text
完整重复 patient case 多份副本
```

## 11.3 Rejected items

可以留在 Workspace。

WorkingView 只需：

```text
rejected refs + rejection reason summary
```

除非当前新证据可能重新打开它。

---

# 12. P2 — Capability Orchestration Tax

真实病例中常出现：

```text
capability.discover
capability.activate(tcm.core)
```

但 tcm.core 并未新增：

```text
skills
knowledge scopes
tools
```

审计 clinical-primary 默认入口。

若 `tcm.core` 实际是默认必选能力，则考虑：

```text
默认 active
```

仅特殊能力：

```text
gaofang
acupuncture
preparation
```

再 discover / activate。

此项优先级低于 P0/P1。

只有确定无临床行为风险时才做。

---

# 13. Completion / Submission Strategy

不新增硬 EarlyStop Engine。

但 Agent/Skill 必须更明确何时进入提交。

建议 WorkingView 提供紧凑的：

```text
Completion Readiness
```

例如：

```text
clinicalCoreReady = true
formulaDecisionReady = true
safetyDispositionKnown = true
remainingDecisionChangingUnknowns = [...]
remainingUnknownsToolResolvable = false
```

这只是 state projection，不是硬流程。

Skill 指导：

```text
如果 core ready，
且 remaining unknowns 不可由当前工具解决，
则提交医生可审阅结果。
```

不继续无效 search。

---

# 14. Mode Stability

H15.4 报告已经记录：

```text
T20 urgent ↔ clinical
T22 clarification ↔ clinical
```

存在 Agent mode 漂移。

本轮不要求把所有 mode 变成完全 deterministic。

但至少修复：

```text
相同结构化 safety state + clinical core 已形成
```

时：

### non-urgent high-severity
不得随机因为“谨慎”退化成 clarification-only。

应：

```text
clinical + reviewRequired=true
```

### urgent
Safety 仍可阻止 normative commit。

不得为 mode consistency 降低 Safety。

---

# 15. 这轮不做什么

明确禁止：

```text
新增 Multi-Agent
新增 Planner subsystem
新增 Verifier Agent
Tree Search
RL
复杂 Context Compression
扩大 step limit
固定 search 次数
固定第 N 步 submit
疾病/症状→方剂 hard map
新增 Modification REMOVE
扩 medication rules
大规模知识库扩建
重新设计 Safety
```

---

# 16. 实施顺序

不要同时乱改。

按以下顺序：

```text
Phase 1
P0 stateful cache correctness

Phase 2
Planner/WorkingView unknown resolvability + context selection

Phase 3
Skill expert convergence hardening

Phase 4
clarification boundary + completion readiness

Phase 5
gaofang execution handoff

Phase 6
retrieval applicability / specialty contamination

Phase 7
dedup + compact tool receipts

Phase 8
capability orchestration tax（仅低风险时）

Phase 9
targeted real-case validation
```

每个 Phase 完成后只跑该机制的 deterministic tests。

最后才跑真实病例。

---

# 17. 必须新增的 deterministic tests

## Test A — Stateful candidate cache

```text
state v1
→ search_candidates
→ result A

change primary pattern/treatment

state v2
→ search_candidates

EXPECT:
not stale reuse of state v1
```

---

## Test B — Static evidence cache

```text
get_source(source X)
get_source(source X)
```

EXPECT：

```text
same source allowed to reuse
second response compact
no duplicate Workspace evidence event
```

---

## Test C — Unresolvable patient info

Input：

```text
病例缺最新 CRP / CT
```

Tool set：

```text
knowledge tools only
```

EXPECT：

```text
CRP/CT stays missing
does not cause repeated knowledge.search
clinical result still allowed if core is sufficient
```

---

## Test D — Candidate de-dup

两个不同 search 都返回同一 formula。

EXPECT：

```text
candidate.presented once
subsequent appearances reused/deduped
```

---

## Test E — Evidence de-dup

同一 evidence 被 3 个 query 命中。

EXPECT：

```text
one canonical evidence item
not 3 duplicated evidence.added events
```

---

## Test F — Deliberation compact receipt

大 deliberation payload 写入。

EXPECT：

```text
tool output << input payload
returns delta / receipt
does not echo full request
```

---

## Test G — Clarification boundary

Input 有少量不可获取 missing info，但：

```text
disease/pattern/treatment/formula
```

已可形成。

EXPECT：

```text
clinical
not clarification-only
missing_information preserved
```

---

## Test H — Gaofang stage decision

明确输入：

```text
以膏代煎
```

并存在：

```text
acute excess + chronic deficiency
```

EXPECT：

必须形成以下之一：

```text
suitable
not suitable
treat first then gaofang
```

不得：

```text
gaofang activated + asset retrieved + no treatment-specific decision
```

---

## Test I — Specialty contamination

65 岁男性肺系病例。

EXPECT：

```text
妇科/妊娠 normative treatment evidence
不进入主要 treatment candidate set
```

允许 AUX symptom evidence 若其语义本身跨 specialty 且 metadata 合理。

---

# 18. 最终真实病例测试

只跑以下 5 例，每例首跑 1 次。

不要 best-of-N。

---

## Case 1 — 宫颈癌术后

目的：

```text
high-severity non-urgent
reviewRequired
clinical advisory preserved
```

EXPECT：

```text
不 clarification-only
不 resource_limit_fallback
formula/advisory preserved
reviewRequired=true
```

---

## Case 2 — 产后出血/恶露不绝

目的：

```text
竞争证型较多
验证 retrieval 是否仍不断扩展
```

EXPECT：

```text
候选数量明显收敛
重复 evidence/candidate 大幅减少
不需要 7 次近义 search
Agent 自行 submit
```

不要求 exact match 原医生多套急救/缓解方案。

重点看：

```text
当前状态识别
主证
基础方合理性
收敛效率
```

---

## Case 3 — 赵某 65岁 咳喘 10 年，加重 1 周，“以膏代煎”

输入：

```text
赵某，男，65 岁，退休工人
初诊日期：2023 年 11 月 5 日
主诉：反复咳嗽喘息 10 年，加重伴咳痰黄稠 1 周。每于秋冬季节发作，近 1 周因受凉诱发，现咳嗽声低，喘息气短，活动后加重，痰多黄稠难咯，伴腰膝酸软，口干咽燥，胸闷腹胀。
辅助检查：肺功能示阻塞性通气功能障碍；胸部 X 线示双肺纹理增粗，肺气肿征象。
舌脉：舌红，苔黄腻，脉沉滑数。以膏代煎
```

重点：

```text
痰热壅肺为当前标实
肺肾两虚为本
```

EXPECT：

```text
1. 给当前治疗方向/方药 advisory
2. 给明确 gaofang stage decision
3. 若判断当前不宜调补收膏：
   明确先治标，后转膏
4. GF-002 如被采用，应作为后续膏方 evidence
5. 不得最终完全无方
6. 不 resource_limit_fallback
7. 不因缺 CRP/血氧等不可获取信息反复 search
```

备注：

这例不是要求 Agent “强行开补膏”。

PASS 是：

```text
当前治疗有交付
+
膏方阶段判断明确
```

---

## Case 4 — T19 经期延长 negative-control

目的：

```text
简单病例不要因为新收敛逻辑变复杂
```

EXPECT：

```text
正常 base formula
无不必要 modification
reviewRequired=false
工具调用不增加
```

---

## Case 5 — T23 多兼症 modification-positive

目的：

```text
确认减少 context / retrieval 后
不会损伤此前正确的临床取舍
```

EXPECT：

```text
仍能识别必要 modification
仍能拒绝与出血倾向冲突的破血候选
不因 early convergence 漏掉关键安全判断
```

---

# 19. 最终记录指标

每例记录：

```text
status
mode
steps
toolCalls
inputTokens
outputTokens
latency
agentProposalSubmitCount
forcedFinalization
resourceLimitFallback

knowledge.search count
formula.search_candidates count
formula.get_evidence count
duplicateEvidenceEvents
duplicateCandidateEvents
reusedToolCalls
noNewInformationCalls

uniqueEvidenceCount
uniqueCandidateCount
focusedCandidateCount

clinicalCoreFormed
formulaSelectionFormed
gaofangDecisionFormed（如适用）
modificationPlanFormed（如适用）

reviewRequired
missingInformationCount
```

---

# 20. 质量指标

人工审查：

```text
Primary Pattern:
PASS / ACCEPTABLE / QUESTIONABLE / FAIL

Treatment Principle:
PASS / ACCEPTABLE / QUESTIONABLE / FAIL

Base Formula:
PASS / ACCEPTABLE_ALTERNATIVE / QUESTIONABLE / FAIL

Clinical Closure:
COMPLETE / PARTIAL / FAILED

Convergence:
CLEAN / MINOR_REDUNDANCY / LOOPING

Gaofang:
APPROPRIATE / NOT_APPLICABLE / FAILED

Modification:
IMPROVED / NEUTRAL / WORSE / N/A
```

---

# 21. 本轮 PASS 标准

本轮不是要求每例固定低步数。

必须同时满足：

## 21.1 Correctness

```text
stateful stale cache = 0 confirmed occurrence
```

---

## 21.2 Clinical closure

5 例中：

```text
不得出现：
病证法已形成
+
可形成 advisory
+
最终却完全无方/无治疗结果
```

除非 deterministic Safety 明确阻止。

---

## 21.3 Convergence

```text
resource_limit_fallback = 0 / 5
```

若出现 1 例，必须证明是真正不可完成，不是重复检索导致。

---

## 21.4 Duplicate reduction

相对旧 trace：

```text
duplicate evidence events
duplicate candidate events
repeated full-payload echoes
```

应明显下降。

目标不是漂亮数字，而是相同内容不再反复进入 Workspace / model context。

---

## 21.5 Retrieval discipline

不得再次出现：

```text
为了一个不可通过工具解决的患者检查信息
连续 knowledge search
```

---

## 21.6 Gaofang closure

赵某病例：

```text
必须有当前治疗 advisory
必须有 gaofang stage decision
不得 resource-limit fallback
```

---

## 21.7 Skill clinical quality

减少 search / context 后：

```text
T23 modification safety judgment 不退化
T19 simple case 不变复杂
```

证明不是靠“少想”换速度。

---

# 22. 如果某项仍失败

不要继续连续补丁。

只允许：

```text
若 >=2 例出现同根因 failure
→ 定为系统性残留问题
→ 报告唯一 root cause
```

若只是单例：

```text
记录
不立即扩架构
```

---

# 23. 最终输出报告

生成：

```text
reports/H15.5-clinical-execution-convergence.md
```

建议结构：

```text
1. Executive Result
2. Confirmed Root Causes
3. Code / Skill / WorkingView Changes
4. Cache Semantics
5. Unknown Resolvability
6. Skill Expert Hardening
7. Gaofang Closure
8. Retrieval / Specialty Filtering
9. Dedup / Context Reduction
10. 5 Real-case Results
11. Before / After Efficiency
12. Clinical Quality Regression Check
13. Remaining Issues
14. Final Verdict
```

最终只能给以下之一：

```text
EXECUTION CONVERGENCE BASELINE VERIFIED
```

或：

```text
EXECUTION CONVERGENCE NOT YET VERIFIED
PRIMARY BLOCKER = ...
```

不要为了通过而隐藏失败。

---

# 24. 结束后的项目状态

如果本轮 PASS：

冻结：

```text
Safety semantics
Execution convergence semantics
WorkingView core projection
Core tool surface
Gaofang execution handoff
```

之后不要继续 Runtime 细调。

下一阶段才回到：

```text
扩大真实临床病例
提高 Knowledge coverage
提高 Expert Skill 的临床深度
逼近真实临床 ≥90%
```

本轮的核心不是让系统“少思考”。

而是：

> **让系统只在真正值得思考的地方思考；想清楚之后就交付。**
